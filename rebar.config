{erl_opts, [debug_info, 
            fail_on_warning, 
            {parse_transform, lager_transform}, 
            {i, "include"}
            ]}.

{sub_dirs, ["rel"]}.

{deps_dir, "deps"}.

{deps, [ {lager, "1.*.*", {git, "git://github.com/basho/lager.git", {branch, "master"}}}
       ]}.
